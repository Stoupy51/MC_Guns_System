
# Imports
from beet import FragmentShader, GlslShader, Shader, ShaderPost, VertexShader
from stewbeet import *


# Main function
def main() -> None:
    ns: str = Mem.ctx.project_id

    # Mem.ctx.assets["minecraft"].shaders["name"] = Shader({})
    # Mem.ctx.assets["minecraft"].shader_posts["name"] = ShaderPost({})
    # Mem.ctx.assets["minecraft"].glsl_shaders["name"] = GlslShader("")
    # Mem.ctx.assets["minecraft"].vertex_shaders["name"] = VertexShader("")
    # Mem.ctx.assets["minecraft"].fragment_shaders["name"] = FragmentShader("")

