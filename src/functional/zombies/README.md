
# TODO:
- /stopwatch follow-up: weapon cooldowns (fire rate / reload / switch) are still tick-based — mode timers were migrated to the mgs:clock #tick_delta system
- Hit direction indicator: calibrate HIT_DIR_HEIGHT / HIT_DIR_ASCENT in weapon/hit_indicator.py so the arc rings the crosshair in-game + TODO: modify textures so they are a gradient from white to transparent (then red later)
- Zombies: add a debug/admin menu for changing points, round, etc. accessible for OPs only
- Multiplayer: add a knife with very short range (entity interaction range) as the first hotbar slot. Then weapons should be .1 and .2.
- Multiplayer: add in loadouts the possibility to choose a knife skin/camo like guns do.

# Zonweeb:
- special zombie that gives resistance to near zombies

